# GA4 release — September 16, 2026

Published source: `643ac198e0935edec1b127a275a23cd533693fa4` on
`origin/codex/ga4-setup`, pushed before the HostGator upload.

The root-domain static export includes measurement ID `G-6YWHB69NED`.
Lint, type checks, 51 unit tests, 19 browser tests, and the production export
passed. All ten configured Google calendars passed anonymous availability checks.

## Deployment

A full dated backup of the existing document root, including hidden files, was
created in the account's private `tmp` directory:
`laaw-life-before-ga4-2026-09-16-1140.tar.gz`. This is a server-side backup;
no local download was verified.

Three archives were uploaded into private staging and extracted into the
confirmed `laaw.life` document root in this order:

| Archive | Files | SHA-256 |
| --- | ---: | --- |
| `laaw-ga4-assets-2026-09-16.tar.gz` | 15 | `7cc24975c9109ad1f50910280bcb1ae42afbf4f2212ab7d0d7a668e35b1b0928` |
| `laaw-ga4-pages-2026-09-16.tar.gz` | 17 | `47876f23944604dc8956d65def8a3f46863f84398d5d854c4ce6465032ba31c8` |
| `laaw-ga4-home-2026-09-16.tar.gz` | 5 | `37dd0e49eb17170ebc240918731ce4aa060ee076f9ce58ac2fae2a33b5aa428d` |

This scoped release excludes `.htaccess`, `calendar-data`, and `og`. Existing
hosting rules, live calendar refreshes, the original page, and older hashed
assets were retained. The refresh schedule was not changed.

## Live verification

- Both location routes rendered current events and embedded calendars.
- Navigation from Ivy Station to Hawthorne worked.
- The homepage redirected to the saved location while preserving UTM parameters.
- The live page loaded exactly one external Google tag for `G-6YWHB69NED`.
- GA4 Realtime showed one active user, an Ivy Station page view, `first_visit`,
  and `session_start` after a real browser visit.
- A later Realtime refresh confirmed Hawthorne page views after location
  navigation and the homepage redirect test, plus four total page views
  (Ivy Station, Hawthorne, the homepage, and redirected Hawthorne).
- The owner's new Google login has property-level Administrator access; the
  original administrator remains.

Direct HTTP verification from the terminal returned HTTP 406, so a post-release
byte-for-byte comparison and HTTP status/MIME checks could not be completed
there. Browser verification succeeded. The original `og` files were not included
in any deployment archive.

Outbound-click measurement is enabled, but clicks within the embedded Google
Calendar are outside the parent site's tracking scope. No outbound event was
claimed as verified.
